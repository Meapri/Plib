{
  "name": "debian-arm64",
  "version": "bookworm-slim-2026-05-gimp-materialized-v103",
  "description": "Tiny PoC rootfs with source-built guest GLES/EGL shim API smoke, libEGL/libGLESv2 ABI-name smoke, eglGetProcAddress GLES demo workload, es2gears-like GLES demo workload, source-built Wayland/X11-shaped GUI GPU clients with TCP and Unix socket transports, a source-built minimal WAYLAND_DISPLAY Unix socket display client with registry bind surface buffer commit messages, a dynamically linked glibc simple GUI demo binary using the same Wayland display/AHardwareBuffer path as the next GIMP demonstration staging target, app-private shared-file fallback RGBA payload verification, SCM_RIGHTS memfd continuous eight-frame payload verification, a strict Wayland wire-header subset trace, and a bounded raw binary Wayland request stream for registry/compositor/shm/surface/buffer lifecycle decoding, syscall-heavy benchmark fixture, LD_PRELOAD path fast path shim with glibc stat metadata directory stream realpath mkstemp rename mkdir unlink rmdir and fakeroot chown compatibility, multi-frame clear and triangle draw GLES workloads, TCP loopback guest GPU bridge client, GLES shim TCP and Unix socket submit plus ACK timing paths, GLES shim Unix socket batched command plus single ACK path, clean extraction schema, dpkg-split smoke, apt/dpkg bundle, installed-script smoke, installed-package GPU IPC smoke, installed-package GLES demo smoke, installed-package GLES IPC ACK smoke, installed-package eglGetProcAddress GLES procaddr demo smoke, installed-package Wayland/X11 GUI GPU bridge client TCP and Unix smoke, installed-package minimal WAYLAND_DISPLAY shared-file plus continuous eight-frame memfd client Unix smoke, installed-package glibc simple GUI demo Unix smoke, materialized Debian arm64 GIMP package closure with installed-package version probe smoke, installed-package Vulkan discovery IPC client smoke with structured Android device and feature records plus guest-requested Vulkan Surface clear, a minimal guest libvulkan proxy smoke using a bounded binary bridge frame, a Vulkan ICD manifest discovery smoke, a vulkaninfo-style guest loader discovery smoke, and a Unix-domain Vulkan loader bridge path.",
  "assets": [
    {
      "path": "tiny-rootfs.tar",
      "sha256": "41a737724a1f67c2c9ad0aa31598c770163edc3ab3b0c9d99380ae9ff3e332fd",
      "size_bytes": 473436160
    }
  ],
  "unsupported_features": [
    "systemd",
    "docker",
    "real_chroot",
    "real_bind_mounts",
    "kernel_modules",
    "privileged_device_nodes"
  ]
}
